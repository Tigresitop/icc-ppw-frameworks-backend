databases:
  - name: fundamentos-db
    databaseName: fundamentos_db
    user: ups
    plan: free

services:
  - type: web
    name: fundamentos-api
    env: docker
    plan: free
    healthCheckPath: /api/actuator/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DATABASE_URL
        fromDatabase:
          name: fundamentos-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRATION
        value: 3600000